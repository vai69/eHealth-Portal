<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <form  action = "<%= postURL%>"   method="POST" >
        <p><%= doctor%></p>
        <img src= "data:<%= image1.contentType %>;base64,<%= image1.data %>">
        <% image2.forEach(function(img2){ %>
            <img src= "data:<%= img2.contentType %>;base64,<%= img2.data %>">
        <% }) %>
        
        
        <textarea name="comment" cols="30" rows="10">Your application is being verified successfully</textarea>

        <button type="submit" name="submit" value="success$<%= doctor.email %>" >Success</button>
        <button type="submit" name="submit" value="failure$<%= doctor.email %>" >Failure</button>
    </form>
   
</body>
</html>